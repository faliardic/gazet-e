name: readerproof
description: "Gazet+E one-tap local integration and interactive reader proof."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  crypto: 3.0.7
  flutter:
    sdk: flutter
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fixtures/edition.json
    - assets/fixtures/edition.v2.json
    - assets/images/
